# Run A — Control (cold, no Palate context)

Paste the following into a fresh Claude conversation. Take a screenshot of the rendered output.

---

Build me a single-page website for a heritage boutique hotel called Devigarh, located in a restored 18th-century palace in Rajasthan, India. The site needs: a hero section, a story/heritage section, rooms (4–6 suites), dining, and contact.

The brand voice is quiet, confident, and assumes the visitor knows what they're looking at.

Deliverable: a single self-contained HTML file with all CSS in a `<style>` tag in the head. No frameworks, no Tailwind, no external libraries. Just HTML and CSS. Use placeholder text where appropriate (lorem-style is fine, but write something that fits a heritage palace hotel — don't use literal lorem ipsum).

For images, use placeholder `<div>` blocks with a subtle background color and a label indicating what photograph would go there (e.g., "Photograph: courtyard at dusk"). Do not use external image URLs.

Generate the complete file.
